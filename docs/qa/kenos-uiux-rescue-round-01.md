# Round 1 — Visual Foundation (evidence)

**Direction:** A (Native Content First) + selective B lede / C Continue framing  
**SHA (working tree):** uncommitted rescue changes on top of `502d805c`  
**Preview:** `http://127.0.0.1:5291/?kenosDemo=1`  
**Stamp:** Round 1 foundation only — **not** visual quality pass.

---

## What changed (user-visible)

1. **Typography roles** (`apps/aios/src/app.css`): page / section / list / body / meta / numeric tokens; Display poster size reduced.
2. **Spacing rhythm:** tighter page-top and section gaps; bottom pad accounts for tab bar.
3. **Today:** hub lede from Direction B; smaller title; denser section stack.
4. **Spaces / Inbox (control-page) / Training:** same type + spacing system; Training demo list uses split name/reps + active tint (Direction A Fitness).
5. **Space Switcher:** title **Continue**; hairline list (no raised card rows); current = inset accent bar; resume hint copy.
6. **FAB sticker removed;** replaced by quieter **Continue** control anchored above BottomNav (still temporary — Round 2 will native-ize further).

---

## Evidence

| Item | Path |
|---|---|
| Before (baseline copy) | `output/uiux/.../rounds/r1/before/web-390x844|web-1440x900/` |
| After | `output/uiux/.../rounds/r1/after/web-*/` (38 aios shots) |
| Contact sheets | `output/uiux/.../rounds/r1/contact/r1-*.png` |

### Flows exercised
1. Today load (demo seed)  
2. Spaces list browse  
3. Open Continue / Space Switcher  
4. Inbox control list  
5. Training Push Day list + CTA  

Platforms: Web 390 and 1440 (primary). iPhone/iPad re-shoot deferred to Round 2 shell matrix (still required before Owner review).

---

## Why after is better than before (visual)

| Issue | Before | After |
|---|---|---|
| Poster title | ~72px “Today” dominates | Page-title role (~28–36px) |
| Hub job unclear | Generic status subtitle | Explicit priority lede |
| Sparse section gaps | Large empty bands | Measured `--kenos-section-gap` |
| Switcher cards | Soft raised rows | Hairline groups + leading selected |
| FAB sticker | “SPACE Switch” marketing chip | Quiet Continue control |
| Training list | Glued “name · sets” prose | Split columns + active accent |

---

## Still looks cheap / unresolved

1. Pure dark canvas, almost no materials on real tab/sidebar (Round 2).  
2. Continue control still floats above tab — not yet a native toolbar/sheet-only entry.  
3. Today still has accent marker bars that can read “dashboard widget”.  
4. iPad layout unchanged (mobile stretch).  
5. Domain identity still mostly deep-link / external rows.  
6. Light mode / Dynamic Type not proven this round.  
7. Mobile space-switcher after shot needed manual re-capture (trigger moved).

**Agent internal ceiling this round:** ≤ 72/100 foundation progress — **not** Owner score.

---

## Blind review (Round 1)

See companion note after reviewer pass. Must cite contact sheets; must list ugliest 3 frames remaining.

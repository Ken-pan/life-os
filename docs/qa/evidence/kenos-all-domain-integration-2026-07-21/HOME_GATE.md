# Home Gate — DAILY_BETA_INTEGRATED (+ HomeScan companion)

Embedded `apps/home` Continuity with real IA: **Rooms · Items · Organize · More**.  
`homePath` = `/plan` (not thin `/storage`-only landing). Web BottomNav hidden under `iosNativeShell`; DomainMusicHeader on non-canvas routes.

**Original iOS capabilities** live in companion `ios/home-scan` (HomeScan). Domain More opens `homescan://scan|find|container` via `KenosHomeScanBridge`. Cloud scan pull remains web Settings. Focus: `/tidy/go` hides Domain Dock. Shelf privacy: `hide_interior_images` (no interior thumbs).

See `HOME_IOS_FEATURES.md` for inventory / PARTIAL list.

# Embedded Mode Report

All production domain apps (plan/training/money/library/music/home/health) use Continuity WKWebView + `iosNativeShell` to hide legacy AppBar/BottomNav and show DomainMusicHeader where applicable.

Work embeds on AIOS origin (`/work`). Paper is legacy AIOS placeholder only.

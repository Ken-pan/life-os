# Account Isolation

Continuity resume storage keys remain user-scoped via `domainContinueStorageKey`.  
SpaceSwitcher bindOwner unchanged. No cross-account leak path introduced. Device account-switch smoke still LAN-dependent.

[build]
  command = "npm run build -w health-os"
  publish = "apps/health/build"
  ignore = "bash ./scripts/netlify-ignore-if-changed.sh apps/health packages/theme packages/sync"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "npm run build -w fitness-os"
  publish = "apps/fitness/build"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- apps/fitness packages/theme packages/sync"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
